package de.dariah.schereg;

import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.nio.file.Files;

import junit.framework.Assert;

import org.apache.commons.io.IOUtils;
import org.exolab.castor.xml.schema.reader.SchemaReader;
import org.junit.Test;
import org.xml.sax.InputSource;

public class XmlTest {

	@Test
	public void testSchemaReader() throws IOException {
		//SchemaReader reader = new SchemaReader("S:/Dropbox/DARIAH/vra.xsd");
		//reader.read();
	
		//File f = new File("S:/Dropbox/DARIAH/lido-v1.0.xsd");
		//FileInputStream fin = new FileInputStream(f);
		
		//InputSource src = new InputSource(fin);
		
		//reader = new SchemaReader(src);		
		//reader.read();
	}
}
